public class ScriptDeepFunctionCallsInspector : LuaScript
{
    public ScriptDeepFunctionCallsInspector() : base("Deep Function Calls Inspector", "This script permits to dump in real-time the calls of every function in Elsword with the function name, arguments number, arguments values, return type and return value.", "Dumper", System.Text.Encoding.UTF8.GetString(OpenMoonLoader.Properties.Resources.DeepFunctionCallInspector))
    {

    }
}